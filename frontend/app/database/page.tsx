"use client";

import MemoryList from '../../pages/MemoryList';  // Import the MemoryList component

const App = () => {

    return (
        <div>
            <MemoryList />
        </div>
    );
};

export default App;
